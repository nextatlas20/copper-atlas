def render(bundle):
    """ACTION: documentation is not a marker."""
    # ACTION preserve the calibration legend in exports
    # WATCH: inspect the fallback palette before publication
    return bundle

# WATCHES: longer marker is ignored
