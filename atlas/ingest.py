def ingest(packet):
    # action: reject packets missing a calibration code
    example = "WATCH: this is a string, not a marker"
    # ACTIONS: longer marker is ignored
    # watch   normalize the incoming sensor label
    return packet
